name: CD Pipeline

on:
  push:
    branches: [main]

jobs:
  placeholder-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment placeholder
        run: echo "Deployment pipeline - to be configured"
